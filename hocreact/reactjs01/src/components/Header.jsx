import React from "react";

export default function Header({ title, description }) {
  //   console.log(props);

  return (
    <div>
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
}

//Props
